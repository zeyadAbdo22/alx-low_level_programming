#include "main.h"

/**
 * mul ->
 * is a function that myltiplies two integers.
 *
 * @a: a is an integer parameter
 * @b: b is an integer parameters
 * Return: int.
 */

int mul(int a, int b)
{
	return (a * b);
}
